# Нужно сделать

Исходное ТЗ — [`input.md`](./input.md). Сделанное — [`done.md`](./done.md).

Все три блока реализованы (3 — `4403`, 2 — `errorCode`, 1 — `permissions`), плюс вторая волна
доработок по аудиту (3 read-only агента).

## Открытые вопросы / на решение

- **Контракт settings-пуша (важно, платформе):** `blurImages` и `permissions` едут в одном
  сообщении, каждый пуш заменяет состояние. Пуш **без** блока `permissions` сбрасывает права в
  fail-open. Платформа **обязана** слать полный блок `permissions` в каждом settings-сообщении.
- **Блок 3:** текст экрана `4403` — общий `GatewayRevoked`; нужен ли отдельный текст.
- **Блок 1 — отложенные admin/settings-поверхности** (требуют прав админа канала или почти не
  встречаются; гейт полей ломает инструмент) — список в [`done.md`](./done.md) → «Отложено».
  Решить, гейтить ли: `ManageInvites`, `ManageChatPrivacyType`, `SettingsEditProfile`,
  Collectible/Frozen/AiTone-модалки.
